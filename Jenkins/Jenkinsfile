pipeline{
    agent {
        docker { image 'maven:latest' }
    }   
    stages {
        stage('Build') {
            steps {
                // Run Maven version
                sh 'mvn --version'
            }
        }
    }   
}